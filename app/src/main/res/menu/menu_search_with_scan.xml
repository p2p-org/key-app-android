<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@id/menuItemSearch"
        android:title="@string/common_search"
        app:actionLayout="@layout/item_toolbar_search"
        app:showAsAction="always" />

    <item
        android:id="@+id/itemScan"
        android:icon="@drawable/ic_search_qr"
        android:iconTint="@color/bg_night"
        android:title="@string/qr_scan_hint"
        app:iconTint="@color/bg_night"
        app:showAsAction="always" />

</menu>
