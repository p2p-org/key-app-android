package org.p2p.wallet.intercom

import android.net.Uri

class IntercomDeeplinkManager {
    /**
     * @return true if deeplink was correct and used
     * */
    fun handleBackgroundDeeplink(data: Uri): Boolean = false
}
